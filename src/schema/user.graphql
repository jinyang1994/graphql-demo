# import Post from "post.graphql"

type Query {
  testString: String
  user(name: String!): User
  allUsers: [User]
}

"用户"
type User {
  id: Int
  num: Int
  name: String
  bestFriendId: Int
  bestFriend: User
  followingUsers: [User]
  followingUserIds: [Int]
  posts: [Post]
}
